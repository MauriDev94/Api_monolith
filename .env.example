# Application Environment
APP_ENV=dev

# Database Configuration
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=5432
DB_HOST=localhost

# JWT Configuration
JWT_SECRET_KEY=

# SMTP (optional - for real OTP emails)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_SENDER_EMAIL=
# SMTP_USE_TLS=true
