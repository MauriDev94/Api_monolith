[pytest]
addopts = --strict-markers
markers =
    unit: fast unit tests with isolated logic and mocks.
    integration: component/repository/api integration tests.
    e2e: end-to-end workflow tests.
